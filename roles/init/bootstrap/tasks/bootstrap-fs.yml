- name: Set mirrors
  shell: reflector --verbose --save /etc/pacman.d/mirrorlist --latest 5 --protocol https --sort age

- name: Copy pacman configuratiion file
  copy:
    src: pacman.conf
    dest: /mnt/etc/

- name: Bootstrap fs
  shell: pacstrap /mnt base linux-lts linux-firmware python openssh sudo
