- name: Mount root partition
  mount:
    src: /dev/mapper/root
    path: /mnt
    fstype: ext4
    state: mounted

- name: Mount boot partition
  mount:
    src: '{{target_boot_part}}'
    path: /mnt/boot
    fstype: vfat
    state: mounted
